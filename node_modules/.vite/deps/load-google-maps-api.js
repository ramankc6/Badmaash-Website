import {
  __commonJS
} from "./chunk-RSJERJUL.js";

// node_modules/load-google-maps-api/index.js
var require_load_google_maps_api = __commonJS({
  "node_modules/load-google-maps-api/index.js"(exports, module) {
    var API_URL = "https://maps.googleapis.com/maps/api/js";
    var CALLBACK_NAME = "__googleMapsApiOnLoadCallback";
    var optionsKeys = ["channel", "client", "key", "language", "region", "v"];
    var promise = null;
    module.exports = function(options = {}) {
      promise = promise || new Promise(function(resolve, reject) {
        const timeoutId = setTimeout(function() {
          window[CALLBACK_NAME] = function() {
          };
          reject(new Error("Could not load the Google Maps API"));
        }, options.timeout || 1e4);
        window[CALLBACK_NAME] = function() {
          if (timeoutId !== null) {
            clearTimeout(timeoutId);
          }
          resolve(window.google.maps);
          delete window[CALLBACK_NAME];
        };
        const scriptElement = document.createElement("script");
        const params = [`callback=${CALLBACK_NAME}`];
        optionsKeys.forEach(function(key) {
          if (options[key]) {
            params.push(`${key}=${options[key]}`);
          }
        });
        if (options.libraries && options.libraries.length) {
          params.push(`libraries=${options.libraries.join(",")}`);
        }
        scriptElement.src = `${options.apiUrl || API_URL}?${params.join("&")}`;
        document.body.appendChild(scriptElement);
      });
      return promise;
    };
  }
});
export default require_load_google_maps_api();
//# sourceMappingURL=load-google-maps-api.js.map
